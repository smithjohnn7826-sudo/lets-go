name: 🚀 6-Hour SSHX Discord Notifier (Debian 13)

# -----------------------------------------------------------------
# 1. EVENT TRIGGERS
# -----------------------------------------------------------------
on:
  schedule:
    - cron: '30 0,5,11,16,22 * * *'
  workflow_dispatch:

# -----------------------------------------------------------------
# 2. JOB DEFINITION
# -----------------------------------------------------------------
jobs:
  sshx_max_duration:
    name: Interactive Debug Shell (sshx)
    runs-on: ubuntu-latest
    timeout-minutes: 360 

    container:
      image: debian:13
      options: --user root # Run as root inside container for full permissions

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          apt-get update
          # Install curl, gawk, bash, and procps
          apt-get install -y curl gawk bash procps

      - name: 1. Start SSHX in Background
        id: start_sshx
        shell: bash
        run: |
          echo "Starting SSHX session..."
          
          SSHX_LOG="/tmp/sshx_link.log"
          
          # Download the sshx binary and make it executable
          curl -sSf -L https://sshx.io/get -o /usr/local/bin/sshx
          chmod +x /usr/local/bin/sshx
          
          # Run the binary in the background and redirect output
          > "$SSHX_LOG" 
          /usr/local/bin/sshx run --headless > "$SSHX_LOG" 2>&1 &
          
          # Set the process ID (PID) as an output for cleanup/killing later
          echo "sshx_pid=$!" >> $GITHUB_OUTPUT
          echo "SSHX process started with PID: $!"

      # 🚨 FIX: Polling logic is back, using POSIX [ ... ] for reliable conditional tests
      - name: 2. Wait for and Extract URL (POSIX-Compliant Polling)
        id: extract_url
        shell: bash
        run: |
          SSHX_LOG="/tmp/sshx_link.log"
          SSHX_PID="${{ steps.start_sshx.outputs.sshx_pid }}"
          TIMEOUT=30
          
          echo "Polling log file for URL (max $TIMEOUT seconds)..."
          
          # Loop for a maximum of 30 seconds
          for i in $(seq 1 $TIMEOUT); do
            # Extract the URL
            SSHX_URL=$(grep 'Sharing session at:' "$SSHX_LOG" | gawk '{print $NF}')
            
            # Use POSIX-compliant single bracket [ ... ] test for reliable comparison
            if [ -n "$SSHX_URL" ] && [[ "$SSHX_URL" == "https://sshx.io/s/"* ]]; then
              echo "URL found after $i seconds."
              break
            fi
            sleep 1
          done
          
          # Final Validation and Output
          if [ -n "$SSHX_URL" ] && [[ "$SSHX_URL" == "https://sshx.io/s/"* ]]; then
            echo "sshx_url=$SSHX_URL" >> $GITHUB_OUTPUT
            echo "::notice::Successfully captured SSHX URL: $SSHX_URL"
          else
            echo "::error::Failed to capture sshx URL after $TIMEOUT seconds."
            echo "--- $SSHX_LOG content for final debug ---"
            cat "$SSHX_LOG"
            echo "-------------------------------------------"
            # Kill the background process and fail the step
            kill "$SSHX_PID" 2>/dev/null
            exit 1
          fi

      - name: 3. Send URL to Discord Webhook
        # Now uses the output from the newly renamed step 2
        if: success() && steps.extract_url.outputs.sshx_url != ''
        env:
          URL: ${{ steps.extract_url.outputs.sshx_url }}
        run: |
          WEBHOOK_DATA='{
            "content": "**New SSHX Session Started!** 🚀",
            "embeds": [{
              "title": "Connect to Runner",
              "description": "The 6-hour high-resource debugging session has started. **RECONNECT NOW** to the new link.",
              "url": "${{ env.URL }}",
              "color": 3447003,
              "fields": [{
                "name": "Link",
                "value": "[Click to Connect](${{ env.URL }})",
                "inline": false
              }]
            }]
          }'
          
          curl -H "Content-Type: application/json" \
               -d "$WEBHOOK_DATA" \
               ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: 4. Keep Job Alive for Max Duration
        if: success() 
        run: |
          echo "SSHX session is running in the background. Job will run for the full 6 hours."
          tail -f /tmp/sshx_link.log
